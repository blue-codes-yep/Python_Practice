# Creating a file called init of having the affect of treating the test folder as a package (As in say you have multiple test then it will run the whole folder as a package.)

# So you then could run something like 'pytest test' and it would run the whole folder.
